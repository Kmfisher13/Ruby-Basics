Given the following method:

def my_method(a, b = 2, c =3, d)
  p [a, b, c, d]
end

my_method(4, 5, 6)

Use ruby documentation to determine what this code will print.

my_method(4, 5, 6) => [4, 5, 3, 6]

The answer is found under Core API, calling_methods_rdoc under
"Default Positional Arguments"
